// function letAdamLogin(){
//     let array = []
//     for (let i = 0; i < 1000000; i++) {
//         array.push(i)
//     }
//     return console.log('Acesso Garantido ao Adam')
// }
//se eu fosse ter outra pessoa
const giveAccess = (name) => console.log(`Acesso Garantido a ${name}`)

function authenticate(verify){
    let array = []
    for (let i = 0; i < verify; i++) {
        array.push(i)
    }
}
function letPerson(person, fn){ //os dados que estão sendo passados
    return giveAccess(user)
}

//para não repetir isso vamos usar as higher-Order functions
letUserLogin('siqueira')
letAdminLogin('Julia')
